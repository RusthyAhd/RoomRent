// Firestore Security Rules
// WARNING: These are open rules for development/testing only!
// In production, you should implement proper security rules.

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all documents
    // ⚠️ WARNING: These rules allow anyone to read and write to your database
    // Use only for development and testing
    match /{document=**} {
      allow read, write: if true;
    }
    
    // Example of more secure rules (commented out):
    // match /rooms/{roomId} {
    //   allow read: if true; // Anyone can read rooms
    //   allow write: if request.auth != null; // Only authenticated users can write
    // }
  }
}
